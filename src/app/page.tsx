/**
 * @fileoverview Placeholder dashboard route for CryptoPanel.
 * - Currently renders a stub while feature modules are implemented.
 * - Will later compose market hooks and components from `src/features/market`.
 */
const DashboardPage = () => (
  <main className="flex min-h-[60vh] flex-col items-center justify-center gap-4 p-8 text-center">
    <h1 className="text-3xl font-semibold">CryptoPanel</h1>
    <p className="max-w-xl text-slate-300">
      Dashboard implementation is in progress. Spot prices, history charts, and personalised
      settings will appear here once the feature modules are wired up.
    </p>
  </main>
);

export default DashboardPage;
