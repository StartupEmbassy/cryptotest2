/**
 * @fileoverview Admin route stub for CryptoPanel.
 * - Will display admin-only insights once role-based guards are wired.
 * - Acts as a temporary placeholder during scaffolding.
 */
const AdminPage = () => (
  <main className="flex min-h-[60vh] flex-col items-center justify-center gap-4 p-8 text-center">
    <h1 className="text-2xl font-semibold">Admin Console</h1>
    <p className="max-w-md text-slate-300">
      Admin-specific content will appear here once role-aware components and guards are connected.
    </p>
  </main>
);

export default AdminPage;
