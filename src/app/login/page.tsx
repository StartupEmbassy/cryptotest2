/**
 * @fileoverview Login page stub for CryptoPanel.
 * - Serves as the entry point for Google authentication via Supabase.
 * - Will render the actual login button and redirect logic once auth feature lands.
 */
const LoginPage = () => (
  <main className="flex min-h-[60vh] flex-col items-center justify-center gap-4 p-8 text-center">
    <h1 className="text-2xl font-semibold">Access CryptoPanel</h1>
    <p className="max-w-md text-slate-300">
      Google login via Supabase will be presented here. The authentication flow is pending during
      the initial scaffold phase.
    </p>
  </main>
);

export default LoginPage;
